# ==============================================================================
# Multi-Agent Environmental Analysis System
# Dependencies (Phase 0 & Phase 1)
# ==============================================================================

# Environment configuration management
python-dotenv>=1.0.1

# HTTP client for STAC API metadata querying (Phase 1)
requests>=2.31.0

# Data validation, schema definitions, and settings management
pydantic>=2.7.0

# Type hinting extensions for modern Python runtime type safety
typing-extensions>=4.10.0

# Unit and integration testing framework
pytest>=8.0.0

# NOTE: Heavy geospatial libraries (e.g. rasterio, geopandas) and agent orchestration
# frameworks (e.g. langgraph, langchain) will be introduced in subsequent phases
# (Phases 2-4) when image processing and agent state graphs are implemented.
