# ==============================================================================
# Multi-Agent Environmental Analysis System
# Phase 0: Foundation Dependencies
# ==============================================================================
# NOTE: Only core foundation packages are included at this stage.
# Specialized libraries (e.g. LangChain, LangGraph, rasterio, geopandas)
# will be introduced incrementally during their respective phases.

# Environment configuration management
python-dotenv>=1.0.1

# Data validation, schema definitions, and settings management
pydantic>=2.7.0

# Type hinting extensions for modern Python runtime type safety
typing-extensions>=4.10.0

# Unit and integration testing framework
pytest>=8.0.0
